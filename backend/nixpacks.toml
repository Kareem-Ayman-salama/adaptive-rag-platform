[phases.setup]
aptPkgs = [
  "tesseract-ocr",
  "tesseract-ocr-ara",
  "libgl1",
  "libglib2.0-0"
]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "uvicorn documind_rag.app.main:app --host 0.0.0.0 --port ${PORT}"
